@{
    # A placeholder version; the build framework overwrites this with the version
    # derived from the release tag. Present so the manifest passes analysis.
    ModuleVersion = '0.0.0'
    PrivateData   = @{
        PSData = @{
            Tags = @(
                'Confluence'
                'Atlassian'
                'PSModule'
            )
        }
    }
}
